import { getStudentsQuery } from "../students/__gql__/queries";
import { createStudentQuery, deleteStudentQuery, updateStudentQuery } from "../students/__gql__/mutations"

describe('Test Student', () => {
  it('should return empty array if no students are registered', async () => {
    // cria o servidor de teste
    // cria  o cliente de teste
    // executa a query
    // e verifica se deu certo
    // const apolloServer: any = await createTestServer();
    // const { query } = createTestClient(apolloServer);

    // const res = await query({
    //   query: getStudentsQuery,
    // });

    // expect(res).toBe([]);
  });
});
